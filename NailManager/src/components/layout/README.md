# Layout

Esta pasta guarda componentes de estrutura da interface.

Aqui entram itens como `Header`, `Sidebar`, `Navbar`, `Footer` ou um layout base de uma tela.

Regra simples: se o componente organiza a página, ele fica aqui. Se for um componente genérico como botão ou input, ele vai para `ui`.
