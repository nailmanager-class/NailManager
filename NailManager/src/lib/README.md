# Lib

Esta pasta guarda funções utilitárias e helpers genéricos.

Aqui entram arquivos como `utils.ts`, formatadores, validações genéricas e configurações compartilhadas.

Regra simples: se não é componente, não é hook e não pertence a uma feature específica, provavelmente pode ficar em `lib`.
