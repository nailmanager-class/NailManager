# Services

Esta pasta guarda a comunicação com APIs e regras de acesso a dados.

Aqui entram arquivos como `api.ts`, `auth.service.ts` e outras funções que fazem requisições para o backend.

Regra simples: tudo que busca, envia, atualiza ou remove dados externos deve ficar aqui.
