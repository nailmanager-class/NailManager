# Types

Esta pasta guarda tipos e interfaces do projeto.

Aqui entram definições como `User`, `LoginRequest`, `LoginResponse` e outros formatos de dados usados no sistema.

Regra simples: se o arquivo existe para descrever dados e não para executar lógica, ele deve ficar em `types`.
